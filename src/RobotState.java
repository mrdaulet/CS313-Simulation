import java.util.ArrayList;

public class RobotState {
	
	ArrayList<Pair> lightReadings = new ArrayList<Pair>();
	
	
//	// 1 is black, 0 is white
//	Pair patterns[][] = { 
//			{ new Pair(1, 1), new Pair(1, 0), new Pair(0, 1), new Pair(0, 0) }, // + when turning clockwise 
//			{ new Pair(1, 1), new Pair(0, 1), new Pair(1, 0), new Pair(0, 0) }, // + anti-clockwise
//			{ new Pair(1, 1), new Pair() }, // -| clockwise
//			{}
//		 };
}